# Uptimer Configuration
# Copy to config.yaml and modify as needed

# Authentication (used by both server and CLI)
username: admin
password: admin
secret_key: change-me-in-production

# Server settings
host: 127.0.0.1
port: 8000

# CLI client settings
api_url: http://localhost:8000

# MongoDB storage
mongodb_uri: mongodb://localhost:27017
mongodb_db: uptimer
